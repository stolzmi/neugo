# Trained-model artifacts and prediction output generated by examples —
# regenerate with `go run ./examples/<name>` rather than committing them.
*_model.json
test_predictions.csv

# Standard Go build/test artifacts.
*.test
*.out

# Large real datasets downloaded on-demand by examples/cifar10_cnn and
# examples/cifar100_cnn (~170MB/~160MB from cs.toronto.edu) — re-download
# with `go run ./examples/cifar10_cnn` / `cifar100_cnn` rather than
# committing them. dataset/wine_quality/ is intentionally NOT listed here:
# it's the one small, real dataset this repo does commit.
dataset/cifar10/
dataset/cifar100/

# Exceed GitHub 100MB limit — kept local only (from flax-restructure snapshot)
dataset/test_set/x_test.csv
dataset/train_set/x_train.csv
.superpowers/

# Local Claude Code agent state / worktrees — not source.
.claude/

# examples/cifar10_cnn showcase checkpoint — see model-artifacts comment above.
cifar_10
